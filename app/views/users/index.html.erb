<% provide(:title, 'Users') %>
<ul>
    <% @users.each do |user| %>
    <li>
      <%= gravatar_for(user) %>
      <%= link_to user.email, user_path(user.id) %>
    </li>
    <% end %>
</ul>

<%= will_paginate @users %>
