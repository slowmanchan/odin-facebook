<%= form_for(:liking, url: '/likings/:id@post.id', method: :delete) do |f| %>
  <%= f.hidden_field :post_id, :value => @post.id %>
  <%= f.submit "Unlike", class: 'btn btn-lg btn-primary' %>
<% end %>
